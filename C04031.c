#include <stdio.h>
#include <math.h>
int main(){
    int t;
    scanf("%d", &t);
    int cnt = 1;
    while(t--){
        int n;
        scanf("%d", &n);
        int a[n];
        for (int i = 0; i < n; i++){
            scanf("%d", &a[i]);
        }

    }
    return 0;
}